export type ThumbnailData = {
    title: string;
    link: string;
    thumbnail: string;
    category: string;
};


export type ClientOpinionData = {
    clientName: string
    clientPicture: string
    review: React.ReactNode
}